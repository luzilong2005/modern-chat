export enum PAGE_ROUTE_PATH {
    MAIN = "/main",
    CHAT = "/main/chat",
    HISTORY = "/main/history",
    SETTINGS = "/settings",
    ABOUT = "/about",
}

export enum SETTINGS_ROUTE_PATH {
    APPEARANCE = "/settings/appearance",
    ADVANCED = "/settings/advanced",
}
